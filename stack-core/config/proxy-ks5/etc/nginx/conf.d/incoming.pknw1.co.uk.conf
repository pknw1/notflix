server {
	server_name incoming.pknw1.com incoming.pknw1.co.uk incoming.pknw1plex.co.uk; # This is just an invalid value which will never trigger on a real hostname.
	listen 80;
	access_log /var/log/nginx/access.log vhost;
	#root /var/www/incoming.pknw1.co.uk;
	root /incoming;


	location /error {
		internal;
		root /host/content;
}
	
	location / {
		add_before_body /.hidden/before.txt;
		add_after_body /.hidden/after.txt;
		autoindex on;
	}


}


