# nginx-proxy version : 1.0.1-6-gc4ad18f
# If we receive X-Forwarded-Proto, pass it through; otherwise, pass along the
# scheme used to connect to this server
map $http_x_forwarded_proto $proxy_x_forwarded_proto {
  default $http_x_forwarded_proto;
  ''      $scheme;
}
# If we receive X-Forwarded-Port, pass it through; otherwise, pass along the
# server port the client connected to
map $http_x_forwarded_port $proxy_x_forwarded_port {
  default $http_x_forwarded_port;
  ''      $server_port;
}
# If we receive Upgrade, set Connection to "upgrade"; otherwise, delete any
# Connection header that may have been passed to this server
map $http_upgrade $proxy_connection {
  default upgrade;
  '' close;
}
# Apply fix for very long server names
server_names_hash_bucket_size 128;
# Default dhparam
ssl_dhparam /etc/nginx/dhparam/dhparam.pem;
# Set appropriate X-Forwarded-Ssl header based on $proxy_x_forwarded_proto
map $proxy_x_forwarded_proto $proxy_x_forwarded_ssl {
  default off;
  https on;
}
gzip_types text/plain text/css application/javascript application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript;
log_format vhost '$host $remote_addr - $remote_user [$time_local] '
                 '"$request" $status $body_bytes_sent '
                 '"$http_referer" "$http_user_agent" '
                 '"$upstream_addr"';
access_log off;
		ssl_protocols TLSv1.2 TLSv1.3;
		ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
		ssl_prefer_server_ciphers off;
error_log /dev/stderr;
resolver 127.0.0.11;
# HTTP 1.1 support
proxy_http_version 1.1;
proxy_buffering off;
proxy_set_header Host $http_host;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $proxy_connection;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
proxy_set_header X-Forwarded-Ssl $proxy_x_forwarded_ssl;
proxy_set_header X-Forwarded-Port $proxy_x_forwarded_port;
proxy_set_header X-Original-URI $request_uri;
# Mitigate httpoxy attack (see README for details)
proxy_set_header Proxy "";
server {
	server_name _; # This is just an invalid value which will never trigger on a real hostname.
	server_tokens off;
	listen 80;
	access_log /var/log/nginx/access.log vhost;
	return 503;
}
	# jackett.pknw1.co.uk
upstream jackett.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # jackett
        server 172.18.0.3:9117;
}
server {
	server_name jackett.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://jackett.pknw1.co.uk;
}
}
	# movies.pknw1.co.uk
upstream movies.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # overseerr
        server 172.18.0.2:5055;
}
server {
	server_name movies.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://movies.pknw1.co.uk;
}
}
	# notflix.pknw1.co.uk
upstream notflix.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # overseerr
        server 172.18.0.2:5055;
}
server {
	server_name notflix.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://notflix.pknw1.co.uk;
}
}
	# plex.pknw1.co.uk
upstream plex.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # plex
        server 172.18.0.6:32400;
}
server {
	server_name plex.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass https://plex.pknw1.co.uk;
}
}
	# portainer.pknw1.co.uk
upstream portainer.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # portainer
        server 172.18.0.10:9000;
}
server {
	server_name portainer.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://portainer.pknw1.co.uk;
}
}
	# proxy.pknw1.co.uk
upstream proxy.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # proxy
        server 172.18.0.12:80;
}
server {
	server_name proxy.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://proxy.pknw1.co.uk;
}
}
	# radarr.pknw1.co.uk
upstream radarr.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # radarr
        server 172.18.0.4:7878;
}
server {
	server_name radarr.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://radarr.pknw1.co.uk;
}
}
	# requests.pknw1.co.uk
upstream requests.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # overseerr
        server 172.18.0.2:5055;
}
server {
	server_name requests.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://requests.pknw1.co.uk;
}
}
	# sonarr.pknw1.co.uk
upstream sonarr.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # sonarr
        server 172.18.0.7:8989;
}
server {
	server_name sonarr.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://sonarr.pknw1.co.uk;
}
}
	# sso.pknw1.co.uk
upstream sso.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # auth
        server 172.18.0.11:4200;
}
server {
	server_name sso.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://sso.pknw1.co.uk;
}
}
	# tautulli.pknw1.co.uk
upstream tautulli.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # tautulli
        server 172.18.0.9:8181;
}
server {
	server_name tautulli.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://tautulli.pknw1.co.uk;
}
}
	# utorrent.pknw1.co.uk
upstream utorrent.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # utorrent
        server 172.18.0.5:8080;
}
server {
	server_name utorrent.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://utorrent.pknw1.co.uk;
}
}
	# watch.pknw1.co.uk
upstream watch.pknw1.co.uk {
        ## Can be connected with "proxy" network
        # overseerr
        server 172.18.0.2:5055;
}
server {
	server_name watch.pknw1.co.uk;
	listen 80 ;
	access_log /var/log/nginx/access.log vhost;
	location / {
		proxy_pass http://watch.pknw1.co.uk;
}
}
