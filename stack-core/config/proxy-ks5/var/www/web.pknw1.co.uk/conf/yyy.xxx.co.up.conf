server {
	server_name yyy.xxx.co.up"; # This is just an invalid value which will never trigger on a real hostname.
	listen 80;
	access_log /var/log/nginx/access.log vhost;
	root /var/www/yyy.xxx.co.up/content;
}

