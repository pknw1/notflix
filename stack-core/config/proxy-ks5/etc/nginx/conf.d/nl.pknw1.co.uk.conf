server {
	server_name nl.pknw1.co.uk # This is just an invalid value which will never trigger on a real hostname.
	listen 80;
	root /var/www/pknw1.co.uk/newsletter;


        location / {
                add_before_body /.hidden/before.txt;
                add_after_body /.hidden/after.txt;
                autoindex on;
        }

}

