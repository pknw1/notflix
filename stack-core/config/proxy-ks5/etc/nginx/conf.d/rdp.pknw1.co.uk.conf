server {
	server_name rdp.pknw1.co.uk; # This is just an invalid value which will never trigger on a real hostname.
	listen 80;
	access_log /var/log/nginx/access.log vhost;
	root /var/www/rdp.pknw1.co.uk;

 location / {
                autoindex on;
}

}

